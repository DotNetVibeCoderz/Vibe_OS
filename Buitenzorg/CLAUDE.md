# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

**Attribution:** Buitenzorg OS is made by **Gravicode Studios**, led by **Kang Fadhil**. This credit appears in-OS (boot logo `logo.rs`, the desktop **Welcome** window, and shell `ver`/`about`) and across docs (README, requirements.md, PLAN.md, Progress.md) — keep it consistent when editing those.

**Planning docs:** `PLAN.md` (product roadmap per version) and `Progress.md` (feature checklist tracker) are the living forward-looking docs — update them (and `requirements.md` §17) when a milestone lands. `requirements.md` is the authoritative technical design.

## Project Status

**v0.1–v0.14 milestones ✓ ("Benih" → "Babel").** The kernel boots from BIOS and UEFI in QEMU on all four boot media (`bzimage --media`): framebuffer console, GDT/IDT/PIC interrupts (timer, PS/2 keyboard + mouse), SSE, paging + 16 MiB heap, syscall ABI with a C# mirror, cooperative scheduler with optional preemption, kernel IPC, PCI, IDE/ATA PIO driver, FAT12/16/32 read + FAT12 write, ring-3 NativeAOT C# via SYSCALL/SYSRET, v0.5 system services (VFS, service/init manager, io_uring-style async I/O, loopback Ethernet/ARP/IPv4/ICMP, a C# service process), v0.6 graphics + window system (`gfx.rs` + `wm.rs` compositor), v0.7 desktop environment (`theme.rs` dark/light, 4 workspaces, `terminal.rs`+`shell.rs`, `keyboard.rs`), v0.8 app framework (window syscall ABI, `app.rs` launcher, XOX app), and v0.9 "Serbuk": all **four app variants** (console/desktop/web/widget), a **`Buitenzorg.Drawing`** System.Drawing-style managed graphics library (`userland/hello-csharp/bzdraw.cs`: Graphics/Pen/Brush/Color, shapes via new DrawCmd ops LINE/ELLIPSE/RECT, demo app Paint), and a **Task Manager** (`taskmgr.cs`) backed by a kernel process registry (`process.rs`, CPU-time accounting) and the PROC_LIST/PROC_KILL/SYS_STAT syscalls — it lists processes + uptime/heap/RAM and kills tasks. Widget (`widget.cs`, docked on the widget board via a `widget:` title prefix in `wm.rs`) and a mini WebView (`webview.cs`, subset-HTML renderer) complete the app variants. The v0.10 "Buah" theme engine (`theme.rs`) adds design tokens **plus style params** and **8 built-in themes** (Neo Brutalism, Clean, Material, Bento, Classic Linux, Classic Windows, Sun, BeOS) beyond dark/light — `theme <name|cycle|list>` switches live. A package manager (`pkg.rs`) has a registry + `bz install/remove/list/search`; the shell's `run` is gated on installed state. The v0.11 "Cahaya" release adds a **compute API** (`compute.rs`: SAXPY/blend on a CPU backend, `Backend::Gpu` reserved for a future GPU driver), a **screensaver** (`screensaver.rs`: Starfield/Mystify/Pipes/Marquee/Bouncing/Blank, idle-activated by `desktop_loop`), **wallpapers** (`wallpaper.rs`: built-in patterns + a 24-bit BMP user image loaded from the VFS — `PHOTO.BMP` is generated by `bzimage/build.rs`), **window controls** (min/max/close buttons + rounded corners + normal/minimized/maximized state in `wm.rs`), **micro-interactions** (hover highlight, click ripple, continuous-animation desktop loop), and shell personalization commands (`settings/bg/saver/cursor/anim`). The v0.12 "Nalar" release adds an **AI subsystem** (`ai.rs`: a real char-level bigram LLM, Sobel edge-detect CV, and procedural text-to-image, all CPU-local; `model.rs`: a Hugging Face-style model gallery + `bz model list/pull/info`; shell `ask <prompt>`) and **power management** (`power.rs`: an ACPI parser using the bootloader RSDP → FADT → PM1a_CNT + DSDT `\_S5` scan, with Shutdown [ACPI + QEMU/VBox port fallback, verified powers off QEMU], Restart [ACPI reset + 8042 + triple-fault], and light Sleep [blank + hlt until input]; shell `shutdown/restart/sleep` + `bz power`). The v0.13 "Lapis" release adds **virtualization**: `vmx.rs` detects hardware VT-x/AMD-V (CPUID + VMX MSRs) and picks a backend, falling back to `vmm.rs` — a **software VMM** with a real virtual CPU ("BZVM": fetch/decode/execute, 8 registers, linear guest memory, a stack, CALL/RET, bounds-checked mem access), virtio-style I/O ports (console char/num, disk-write, host-tick guest tools), a RAM virtual disk, and full snapshot/restore. A tiny guest OS ("NanoOS", assembled by a two-pass in-kernel assembler) actually boots and runs on it; the Virtualization Manager registry drives `vm list/create/start/snapshot/restore/remove` (shell `vm` + `bz vm`/`bz virt`). The v0.14 "Babel" release adds a **polyglot runtime** (`script.rs`): one in-kernel tree-walking interpreter (shared AST/evaluator + a **uniform host binding API** where `print`/`console.log` land on the same `emit`) with three front-ends — **JavaScript** (brace/`;` lexer+parser), **TypeScript** (a real transpile: strip `interface`/`type`/`: Type` annotations → JS), and **Python** (indentation lexer with INDENT/DEDENT + parser); supports functions+recursion, if/else(/elif), while, `for` (C-style and `range()`), operators, string `+`, with a step/output budget so a script can't hang the boot. Shell `script <lang> [file]` + `js`/`ts`/`py` + `bz script`; app templates in `sdk/templates/{js,ts,python}-app`; the same fib program runs in all three languages alongside the C# apps. **v0.15 "Matang" is in progress** (managed runtime C# with GC + full .NET BCL): increment 1 landed the **user memory PAL** — new syscalls `MMAP`/`MPROTECT`/`MUNMAP` (ABI 13–15, `memory.rs` arena at `0x2000_0000`, shim `bz_mmap`/etc., reset per process in `app::run_image`), verified end-to-end from ring-3 C# (`userland/hello-csharp/matang.cs`). Increment 2 landed **cooperative ring-3 threads** — syscalls `THREAD_CREATE`/`THREAD_JOIN`/`THREAD_EXIT` (ABI 16–18): each thread is a kernel task with its **own SYSCALL kernel stack** (kept separate from the TSS interrupt stack so a timer taken during one thread's ring-3 never lands on another's suspended syscall frame — no gdt/TSS changes), and the per-thread longjmp/user-rsp context is swapped around the cooperative `switch_with` (so non-threaded apps are unaffected — the hook is inert unless a task has `.user`). Verified from C# (`thread.cs`: main + a worker sharing the address space each bump a shared counter 1000× and join → `MILESTONE: THREAD OK`). This also fixed a latent bug: the user-side syscall inline-asm must declare **`r8`/`r9`/`r10` clobbered** (the kernel entry marshals args through them), else a value kept there across a syscall is corrupted. (A v0.16 follow-up found the same class of bug on the **argument registers**: `rdi`/`rsi`/`rdx` must be `inlateout(...) => _`, not plain `in` — the kernel restores only rcx/r11/rsp before `sysretq`, and LLVM kept the heap-chunk size in rdi across the mmap syscall, corrupting `HEAP_CAP` so a second heap growth never triggered → USER page fault in the first app needing >1 MiB of managed heap.) Increment 3 added the **sync/TLS/clock PAL**: `FUTEX_WAIT`/`FUTEX_WAKE` (ABI 19–20) introduce a scheduler **`Blocked`** state so waits truly block instead of busy-yielding (the shim builds `bz_mutex_lock`/`unlock` on top), `THREAD_SELF` (21, pthread_self/TLS foundation), and `CLOCK_MONO` (22, TSC). Verified from C# (`sync.cs`: two workers contend on a futex-backed mutex with a genuine mutual-exclusion check — stamp own id in the CS, yield, verify unchanged — plus per-thread-id and monotonic-clock checks → `MILESTONE: SYNC OK`). Increment 4 delivered a **real managed heap**: zerolib's `new`/`RhpNewArray` route through `SystemNative_Malloc` (confirmed by disasm), so making that a **growable, zeroed bump heap** (a .bss arena that grows via `mmap`) plus libc `malloc`/`free`/`calloc`/`realloc` (`bzstart.rs`) makes **`new`, arrays, heap objects, and generics work in ring-3 C#** — verified (`heap.cs`: `new int[100000]` forcing mmap growth + a heap-object linked list + a generic type → `MILESTONE: HEAP OK`). **Still unsupported: static reference fields** (GC statics uninitialized under zerolib — keep heap state in locals, not `static` refs); and there is no reclaiming GC yet (bump-only). Full GC + static-ref init comes with the NativeAOT `--stdlib:dotnet` link. Increment 5 added the **GC memory model**: `MMAP` with `prot=NONE` now **reserves** address space without committing frames, and `MPROTECT` with access **commits on demand** (maps fresh zeroed frames for reserved-but-unmapped pages) — the reserve-then-commit pattern the .NET GC uses for its heap (verified: `gcmem.cs` reserves 256 MiB lazily without OOM, then commits pages → `MILESTONE: GCMEM OK`). Increment 6 added **`Buitenzorg.Bcl`** (`userland/hello-csharp/bzbcl.cs`) — a hand-written .NET-style library on the working heap: `BzList<T>`/`BzStack<T>`/`BzQueue<T>`/`BzIntMap<V>`/`BzStrMap<V>` (Dictionary)/`BzIntSet` (HashSet)/**`BzRefList<T>`** (a linked-list-backed list for *reference* element types, avoiding the `stelem.ref` object-array fault), LINQ-style `Where/Select/Sum/Count/Any/All/Max/Min/Contains/IndexOf/Reverse` + `First/Last/Take/Skip/Average/Aggregate`, a `BzStringBuilder` (dynamic numeric output + `AppendHex`/`AppendLine`/`Clear`/`CopyTo`), `BzBitConverter`, `BzBase64`, `BzHex`, `BzMath` (Abs/Clamp/Pow/ISqrt/Gcd/…), `BzRandom` (xorshift64* PRNG), `BzConvert` (ParseInt/Long/Hex + `LongToChars`), and `BzStr` (Equals/StartsWith/EndsWith/IndexOf/char classification — using `(object)` casts since `string ==` needs the missing `op_Equality`) (verified: `bcl.cs` → `MILESTONE: BCL OK`). Real, usable collections/text/encoding in ring-3 C# — **not** the official CoreLib (LINQ/Regex/Tasks proper still need the `--stdlib:dotnet` link, deferred: it links dynamically to glibc + needs bflat's internal entry glue, so a static freestanding link is a multi-session effort). **Gotcha for BCL authors:** a method-group→delegate conversion (`SomeMethod` passed where a delegate is expected) caches the delegate in a **GC static** field → faults under zerolib; pass **function pointers** (`delegate*<int,bool>` + `&Method`) instead, which are plain code addresses. **v0.16 "Panen" started** with `Buitenzorg.Drawing` as a **client-side software renderer** (`userland/hello-csharp/bzgfx.cs`): the app renders every primitive in C# into a managed `Bitmap` (`uint[]` ARGB) — `Color`/`Graphics` with fill/outline rect, thick lines, circles/ellipses, scanline `FillPolygon`, vertical gradient, `DrawImage`, and alpha blending, **2D transforms** (`Matrix` Translate/Scale/Rotate, fixed-point sin/cos), **`GraphicsPath`** (+`FillPath`/`DrawPath`), **24-bit BMP load/save**, **clipping** (`SetClip`/`ResetClip`), **hatch fill**, **`DrawImageScaled`**, **`DrawString`/`MeasureString`** with an embedded 8×8 `Font` (glyphs built in the ctor into an instance field, since static ref fields don't work), and **visual-enhancement primitives** (`FillRoundedRectangle`/`DrawRoundedRectangle` with coverage-anti-aliased corners, `FillRoundedGradientV` for buttons, `DrawShadow` soft drop shadows, `FillGradientH`/`FillGradient`, `FillCircleAA`) used to make the UI toolkit look modern. **`Buitenzorg.UI`** (`userland/hello-csharp/bzui.cs`) is a retained-mode WPF/Avalonia-style toolkit on top: a `UIElement` visual tree (children as a **linked list**, not `object[]` — see the gotcha below), a Measure/Arrange layout pass (`StackPanel` + `Grid` fixed/star + `Canvas` absolute, explicit Width/Height overriding stretch), hit-testing (`HitTest` is virtual so tab strips / combo dropdowns can extend their hit region), **mouse-event routing** (`UIHost.Mouse` dispatches hover Enter/Leave + press/capture/click/drag to the hit element), and a full base control set (`TextBlock`/`Button`/`CheckBox`/`ProgressBar`/`Border`/`Slider`/`RadioButton`+`RadioGroup`/`ListBox`/`TextBox`/`Menu`/`ComboBox`/`TabControl`/`TreeView` [`TreeNode` hierarchy with expand/collapse]/`ScrollViewer` [clip + scroll thumb]/`DataGrid` [linked-list columns+rows]), all rendered into a Bitmap by a `UIHost` compositor and blitted (verified: `ui.cs` builds a basic window + an "advanced controls" window exercising combo/tab/tree/scroll/grid → `MILESTONE: UI OK`, screenshot `docs/img/desktop-ui.png`). **Two more zerolib gotchas surfaced here:** `string ==` needs `System.String.op_Equality` which zerolib lacks (`error CS0656`) — compare reference types by reference or char-by-char; and a `DataGrid` row's cells must be a linked list, not `string[]`, for the same `stelem.ref` reason. A later pass **deepened the toolkit**: a **popup/overlay layer** (`UIElement.RenderPopup`/`PopupHitTest` + `UIHost.WalkPopupHit`) renders a ComboBox dropdown on top of the whole tree and hit-tests it first, so a popup drawn over sibling controls actually receives the click (the in-tree "last-hit-wins" problem); and the controls were **re-themed** with the new Drawing primitives (rounded gradient buttons with drop shadows, pill sliders/progress bars with AA thumbs, rounded checkboxes/combos) for a modern look. **Another zerolib gotcha found here:** storing a reference into an *object-array element* (`stelem.ref` → `RhpStelemRef`, the array-covariance check) faults, while an object *field* store (`RhpAssignRef`) works — so keep collections of references as linked lists / field stores, not `object[]`. These renderers blit the finished frame to a window with one **`BLIT`** draw op (ABI `draw_op` 7; `wm::blit_on_window`), the WPF/Avalonia compositor model (verified: `draw.cs` → `MILESTONE: DRAW OK`, screenshot `docs/img/desktop-drawing.png`). v0.16 also adds the **OS audio subsystem** (`audio.rs`): an **AC'97 driver** that enumerates the sound card on PCI (class 0x04/0x01), cold-resets the codec, programs the mixer (master + PCM-out volume, verified by write/readback), and plays 16-bit stereo PCM at 48 kHz through the **bus-master DMA** engine via a buffer-descriptor list — with new syscalls `AUDIO_STAT`/`AUDIO_SET_VOLUME`/`AUDIO_TONE`/`AUDIO_PLAY` (ABI 23–26, `AudioInfo` struct) and a managed **`Buitenzorg.Audio`** library (`bzaudio.cs`: `Mixer` volume/mute/beep/play + `Tone` generator). QEMU attaches `-device AC97,audiodev=none` on every boot medium; the DMA current-index register advances as buffers drain, confirming real playback (verified: kernel 440 Hz tone + `audio.cs` volume round-trip 45→45 + PCM stream → `MILESTONE: AUDIO OK`, 4-media). An **audio-settings panel** (`audiopanel.cs`) ties it together: a `Buitenzorg.UI` window (volume `Slider` + mute `CheckBox` + test-tone `Button`) wired to the live `Mixer` — slider→`SetVolume`, checkbox→`Mute`, button→`Beep` (verified: slider→device 35, mute→device 0 → `MILESTONE: AUDIO PANEL OK`). Mic capture, IRQ-driven mixing, Intel HDA, per-app mixing, and device selection are the follow-ups. v0.16 also starts the **preloaded suite** (`panen_suite_demo`): a **Calculator** (`calc.cs` → `CALC.ELF`) — a `Buitenzorg.UI` `Grid` of themed digit/operator buttons over a numeric display, with clicks dispatched by `Button.Tag` into a `Calc` engine (verified: simulated `12 + 3 =` → display 15 → `MILESTONE: CALC OK`). The display uses `Graphics.DrawChars` (a char[] overload) to draw dynamic numbers without a managed string. A **2048** game (`game2048.cs` → `G2048.ELF`) follows: a `Board2048` custom `UIElement` renders a 4×4 grid of color-by-value rounded tiles with centered numbers, and a slide/merge engine handles moves (verified: `2+2`→4, `4 4 8 8`→`8 16` → `MILESTONE: GAME OK`, screenshot `docs/img/desktop-2048.png`), and a **Clock** (`clock.cs` → `CLOCK.ELF`): an analog face (AA circle + 12 ticks + hour/minute/second hands rotated with `Graphics.SinFx`/`CosFx`) plus a digital `HH:MM:SS` readout (`MILESTONE: CLOCK OK`, screenshot `docs/img/desktop-clock.png`), and a **Piano** (`piano.cs` → `PIANO.ELF`): a one-octave keyboard (white keys with labels + black keys) whose clicks play notes through `Buitenzorg.Audio` (`Mixer.Beep`), verified by a simulated C-E-G-C arpeggio (`MILESTONE: PIANO OK`, screenshot `docs/img/desktop-piano.png`), and an **App Store** (`store.cs` → `STORE.ELF`): a `StoreView` custom `UIElement` renders an app catalog (name + category + a green/gray install-status badge) with a PASANG/HAPUS button — **wired to the kernel package manager**: the catalog comes from the `PKG_LIST` syscall (registry + install state) and the button installs/removes via `PKG_SET` (which gates the shell's `run`); the demo installs a package and re-reads the registry to confirm the kernel state persisted (`MILESTONE: STORE OK`, screenshot `docs/img/desktop-store.png`). This added the `PKG_LIST`/`PKG_SET` syscalls (ABI 27–28, `PkgInfo` struct) and expanded `pkg.rs`'s registry (suite apps + a `category` field). A **File Manager** (`filemgr.cs` → `FILES.ELF`) browses the VFS through a new `FS_LIST` syscall (ABI 29, `FsEntry` struct — an empty path lists the mounts, a mount path lists its files via `vfs::list`), with a `FileView` custom `UIElement` (path bar + folder/file rows + `..` navigation), verified by listing `/disk` and confirming a known file (`MILESTONE: FILES OK`, screenshot `docs/img/desktop-files.png`). Finally a **Text Editor** (`editor.cs` → `EDITOR.ELF`): a `TextArea` custom `UIElement` with a growable char buffer, a caret, `\n` line wrapping, and Type/Newline/Backspace ops under a menu bar (`MILESTONE: EDITOR OK`, screenshot `docs/img/desktop-editor.png`). An **Image Viewer** (`imgview.cs` → `IMGVIEW.ELF`) rounds out multimedia: it loads `/disk/PHOTO.BMP` through a new **`FS_READ`** syscall (ABI 30 — read a VFS file's bytes into a client buffer; also the foundation for file-open in the editor and other apps), decodes it with Drawing's `Bmp.Load`, and shows it in an `ImageView` custom `UIElement` (fit-to-box with aspect ratio over a checkerboard backdrop + a name/size caption) (`MILESTONE: IMGVIEW OK`, screenshot `docs/img/desktop-imgview.png`). The preloaded suite is now **eight apps** covering every category — productivity (Calculator, Editor), game (2048), utility (Clock, File Manager), multimedia (Piano, Image Viewer), store (App Store). `Buitenzorg.Drawing` gained a **baseline JPEG decoder** (`Jpeg.Load` in `bzgfx.cs`): JFIF/SOF0 sequential DCT, Huffman entropy decode, dequantize, an **integer (float-free) separable inverse DCT**, YCbCr→RGB, chroma subsampling (4:4:4/4:2:2/4:2:0), and restart markers — enough for typical baseline `.jpg` (progressive/arithmetic/CMYK return null). All state is **flat value-type arrays** (Huffman/quant/planes packed into single `int[]`/`byte[]` with per-table offsets) because zerolib faults on `stelem.ref` (jagged arrays / arrays of class instances). The Image Viewer is format-aware (dispatches on `BM` vs `0xFFD8`). Verified by `jpgtest.cs` decoding a 64×64 gradient JPEG (embedded via `build.rs`, generated by ffmpeg) against the ffmpeg reference pixels → `MILESTONE: JPEG OK`.

**Pre-v1.0 BCL completion ("lengkapi BCL", 2026-07-24):** `userland/hello-csharp/bzbcl2.cs` adds the remaining .NET namespaces on top of `bzbcl.cs` — **`System.IO`** (`BzPath`/`BzFile` read+**write**/`BzDir`/`BzMemoryStream`), **`System.Text`** (`BzEncoding` UTF-8 1–4 byte + surrogates, ASCII), **`System.Text.RegularExpressions`** (`BzRegex`: literals, `.`, classes+ranges, anchors, `* + ?`, `|`, groups, `\d \w \s`; IsMatch/Match/Replace/Split — an explicit continuation stack `BzRxCont` makes backtracking correct *across* a group boundary, so `(a|ab)c` matches both `abc` and `ac`; no backreferences/lazy/`{n,m}`/lookaround/captures), **`System.Globalization`** (`BzCulture` grouped ints, fixed-point decimals without floats, byte sizes, month names) plus **`BzDateTime`** on a real CMOS clock, **`System.Diagnostics`** (`BzStopwatch` over TSC, `BzProcess` over PROC_LIST/KILL, `BzDebug`), **`System.Management`** (`BzSystemInfo` over SYS_STAT+AUDIO_STAT), **`System.Net`/`Sockets`** (`BzIPAddress`/`BzSocket`/`BzNetInfo` over **real UDP**), **`System.Threading.Tasks`** (`BzTask`/`BzMutex`), **`System.Timers`** (`BzTimer`, polled), **`GC`** (`BzGC`), and **`Pkg`** (`BzPkg`). Verified end to end by `bcl2.cs` → `MILESTONE: BCL2 OK`, 4-media smoke clean.

This added syscalls **`FS_WRITE`** (32), **`CLOCK_RTC`** (33) and **`NET_SOCKET`/`BIND`/`SEND`/`RECV`/`CLOSE`/`INFO`** (34–39, `COUNT` = 40) with structs `RtcTime`/`NetDatagram`/`NetInfo`; kernel-side, a new **`rtc.rs`** (CMOS RTC, BCD/binary + 12/24 h, re-read until two samples agree — `wm.rs`'s tray clock now calls it) and **UDP in `net.rs`** (per-socket port + backlog, correct IPv4 pseudo-header checksum, `IPPROTO_UDP` dispatch; `NET_SEND`/`NET_RECV` call `net::poll()` so the loopback delivers synchronously).

**Two limits here are real and deliberately not papered over:** (1) there is **no TCP**, so `sock_kind::STREAM` is rejected and `BzHttp` is a *message* layer (build request / parse response) rather than a client — it becomes one the moment TCP lands; the device is also still loopback (no e1000 yet). (2) `BzGC.Collect()` returns **false** — the ring-3 heap is still bump-only, so the statistics are real measurements (via a new `bz_heap_stats` in `bzstart.rs`) but nothing is reclaimed. **A useful discovery:** a C# **function pointer** (`delegate*<ulong,void>` + `&Method`) *does* work as a ring-3 thread entry under zerolib — it is only `[UnmanagedCallersOnly]` that fails — which is what makes `BzTask.Run` real. `bcl2` runs late in the boot sequence (after the audio demo) so `System.Management` and `System.Net` inspect live subsystems.

**BCL adoption audit (2026-07-24):** seven apps were moved onto the BCL where it actually pays: `clock.cs` now shows the **real CMOS time plus a date line** (it used to hardcode 10:08:37), `taskmgr.cs` and `widget.cs` use `BzProcess`/`BzSystemInfo` — **deleting two hand-written copies of the `ProcInfo`/`SysStat` ABI structs**, which were a second place needing a manual edit on every ABI change — `store.cs` uses `BzPkg` instead of decoding `PkgInfo` with pointer arithmetic, `filemgr.cs` uses `BzDir`, `imgview.cs` uses `BzFile`/`BzPath` (the caption name is now derived from the path rather than written twice), and `editor.cs`'s FILE menu became real: Ctrl+S/Ctrl+O save and load `/ram/NOTE.TXT`, with a save→read-back round trip folded into `MILESTONE: EDITOR OK`. Seven others (`calc`, `game2048`, `piano`, `paint`, `webview`, `xox`, `audiopanel`) were deliberately left alone — linking ~30 KB of library for one local number formatter is a net loss. **The audit also turned up two real bugs:** `scripts/build-hello-csharp.sh` (the Linux/CI path) was missing `imgview` and `jpgtest` — they had only been added to the `.ps1`, so a Linux build produced an image without those ELFs and CI would fail on their markers; both scripts now build the same 27 programs. And `sdk/templates/desktop-csharp/README.md` still told app authors to avoid `new T[]`, which stopped being true in v0.15 increment 4. MagicAppGen's `GetApiReference("bcl")` and system prompt now carry the full `bzbcl2.cs` catalogue (otherwise the assistant writes code unaware these namespaces exist), and `docs/first-app.md` gained a namespace table.

The **Editor and File Manager are interactive when shell-launched** (`run editor` / `run files`): they enter a live `KEY_READ` loop — the editor types/backspaces/newlines, the File Manager moves the selection (W/K/S/J), opens folders (Enter), and goes up (Backspace); ESC exits. They tell boot-demo from a real session via a new **`IS_INTERACTIVE`** syscall (ABI 31 — `crate::interactive`, an `AtomicBool` set true just before `desktop_loop`): 0 during the headless boot demo (they skip the loop and exit so boot never blocks), 1 once the desktop is up. The loops **busy-poll** `bz_key_read` (not `bz_yield`) — a non-threaded app's `SYS_YIELD` would context-switch on the shared SYSCALL/privilege stack.

## Debugger + profiler (v1.0)

**Debugger:** `scripts/debug-kernel.ps1`/`.sh` boot QEMU **paused** with a GDB stub on `tcp:1234` and attach GDB against the un-stripped kernel ELF (`kernel/target/x86_64-unknown-none/release/bzkernel`), sourcing `scripts/debug-kernel.gdb` (helpers `bz-break-main`, `bz-faults`, `bz-regs`). Rust symbols are mangled but GDB demangles them, so `break kernel_main` works. If `gdb` isn't on PATH the script still starts QEMU and prints the manual attach commands. The manual path is still `QEMU_EXTRA="-s -S"` + `target remote :1234`.

**Profiler:** `profile.rs` is an instrumented TSC zone profiler — `profile::Guard::new("name")` times a scope to its `Drop` and accumulates cycles per name. It is **inert unless `profile::enable()` was called** (one atomic load when off), so instrumentation left in the tree never perturbs boot timing — do not profile-enable during normal boot. Hot paths already carry zones (`syscall` in `dispatch_from_user`, `wm::compose` + `fb::present` in `present_now`). Shell command `prof [self|on|off|reset|report]`; the report goes to serial. `profiler_demo()` in `main.rs` verifies it deterministically (nested zones, 20× cost ratio, exact call counts, disabled-work-absent) → `MILESTONE: PROFILER OK`. It measures **inclusive** scope time; split child work into its own zone for self-time (that's why compose/present are separate). Full guide: `docs/debugging.md`.

## Syscall pointers from ring 3 are untrusted (v1.0 hardening)

Every pointer argument arriving from ring 3 is validated before the kernel touches it. `memory::validate_user_range(ptr, len, need_write)` requires the range not to wrap, to sit entirely below `USER_ADDR_MAX` (0x8000_0000 — every kernel mapping, including the heap at 0x4444_4444_0000, is above it), and **every page in it to be present and `USER_ACCESSIBLE`** (plus writable for output buffers). `validate_user_cstr` does the same for NUL-terminated paths, re-checking at each page boundary. Both are wired into every pointer-taking syscall.

**What this closed:** an unprivileged app could previously pass a *kernel* address and have the kernel read it back out through `DEBUG_WRITE` (information leak) or **write syscall results into kernel memory** through `SYS_STAT`/`PROC_LIST`/`PKG_LIST`/`FS_READ`/`NET_RECV` — an arbitrary kernel write, i.e. full privilege escalation. An unmapped pointer was fatal a different way: the kernel page-faulted inside the syscall and died. The old `sys_win_create`/`sys_debug_write` TODOs ("full address-space validation arrives with multi-process isolation") are now honoured.

**Important:** validation applies only on the ring-3 path. `usermode::syscall_dispatch_c` calls `syscall::dispatch_from_user`, which sets a flag the validators consult; kernel-internal callers of `dispatch` (boot self-tests, the shell) legitimately pass their own addresses and stay trusted. Getting this wrong is immediately obvious — the kernel's own `syscall_smoke_test` passes a `&str` in kernel rodata to `DEBUG_WRITE`, so an unconditional check panics the boot at `main.rs`'s `assert_eq!(written, msg.len())`. Verified headlessly by `syscall::security_self_test()`, which fires 14 hostile probes (kernel addresses, unmapped pages, ranges overflowing the user half, wrapping lengths, null) and requires every one to be refused with `INVAL` → `MILESTONE: SECURITY OK`.

**ABI v1 is frozen.** `abi_v1_is_frozen` (Rust) and `AbiV1IsFrozen` (C#) pin `ABI_VERSION`, `COUNT`, the size *and* alignment of all ten boundary structs, and the error codes, so a renumbering or field-width change fails in tests before it can reach a released image. Adding a syscall = append + bump `COUNT` + extend both tests; changing an existing one = a new ABI major version.

**Kernel gotcha (hard-won): the privilege/SYSCALL stack must be large (64 KiB, `gdt.rs` `PRIV_STACK_SIZE`).** `PRIVILEGE_STACK` doubles as the TSS rsp0 interrupt stack **and** the SYSCALL kernel stack for a non-threaded app's main thread (`usermode.rs` sets `SYSCALL_KSTACK_TOP` to its top). The deepest thing that runs on it is a `WIN_PRESENT` syscall → `wm::present_now` → `compose_into`, which composites every open window (title bars, Noto text, canvas blits) — with ~20 windows open after the boot demos, the old 20 KiB (`4096*5`) overflowed into adjacent statics **intermittently** (depending on binary layout), smashing return addresses → a breakpoint/#PF/#DF cascade + rodata dump on serial. This looked exactly like the `from_raw_parts`-over-user Heisenbug but was a *different* latent bug (a too-small stack), and it re-triggered whenever a change shifted the layout (e.g. adding the interactive apps). If you see that cascade again, suspect a kernel stack overflow, not just user-memory slices. Relatedly, `wm::present_now` composites into a **reused static `PRESENT_BUF`** (not a fresh 3.7 MiB `Vec` each call) so repeated presents from an interactive app don't fragment/exhaust the heap, and the kernel heap is **32 MiB** (the desktop's back buffer + `PRESENT_BUF` + ~20 window canvases + RAM disks crossed 16 MiB). Suite apps live on `/disk`, so their milestones only appear on the IDE boot medium (the kernel's ATA-PIO driver can't read `/disk` on ahci/nvme/usb) — put app-side markers in the IDE-only smoke list, kernel-side ones (`SUITE OK`) in the shared list. **Kernel gotcha (cost a nasty Heisenbug):** never build a `&[u8]`/`&[u32]` slice with `from_raw_parts`/`from_raw_parts_mut` over a *user-space* buffer — the optimizer then assumes the whole region is dereferenceable/immutable and it corrupted the boot (scrambled serial/rodata dump, page/double faults). This is **layout-sensitive**: it stayed latent for many builds and re-triggered the instant a new kernel function (`panen_suite_demo`, added for the app suite) shifted the binary. The robust fix (`syscall.rs`): a `copy_user_bytes` helper reads the user buffer **per element with `read_volatile`** into a kernel `Vec`, and every string syscall (`DEBUG_WRITE`, `WIN_CREATE`, `WIN_CMD` `DRAW_TEXT`) now works from that copy; `PROC_LIST` writes results out with `write_volatile` instead of a `&mut` slice; the `BLIT` path already read per-pixel with `read_volatile` and passes a raw pointer. Do not reintroduce a slice over user memory in a syscall. Investigation confirmed the path: bflat's `--stdlib:dotnet` provides the real BCL + GC (`libRuntime.WorkstationGC.a`), gated behind a ~200-symbol glibc/pthread/Linux-syscall PAL + threads; next increments build the rest of the PAL (sync/futex/TLS/time/stdio → link the runtime → real LINQ/Regex/Tasks). Also: C# `[UnmanagedCallersOnly]` function pointers don't resolve correctly under bflat zerolib yet (thread entries use a shim-provided native worker for now). Still missing: **the rest of the .NET PAL (sync/TLS/GC init) + thread pool + full V8/CPython engines + ring-3 native polyglot runtime** (needs CoreCLR/GC), **native VMX/SVM driver (VMXON/EPT/VMCB)** + nested HW virt (not exposed under QEMU/TCG), production LLM (GGUF/GPU) + inference scheduler + CV/GenAI audio-video, real GPU driver, APIC, NVMe/AHCI/USB + e1000 drivers, journaled FS, TCP/UDP, tiling WM, full HTML/CSS/JS engine, ACPI S3, **CoreCLR/JIT + GC + reflection** (apps are freestanding zerolib/NativeAOT, no heap — see below).

## Boot is slow (~54 s to READY)

`kernel_main` runs a sequence of milestone demos, now including ~19 ring-3 C# apps (boot demos + the v0.16 audio/suite apps) and full-screen compositor renders — boot takes **~58 s to READY** (an optimization pass trimmed the demo apps' animation `Sleep()` durations from ~12 s down to ~3 s; a much earlier pass cut per-theme renders in `buah_demo`). About half the wall-clock is spent with interrupts off (ELF loading via the IDE-PIO driver + the full-screen `wm::render_frame` passes), which the timer-tick count under-reports; the kernel prints its tick-based estimate at the end of boot. The smoke-test READY wait is 130 s (it breaks as soon as READY appears). When adding demos, avoid extra full-screen `wm::render_frame` calls and long app sleeps, or boot time balloons. To verify a power action (shutdown/restart) without breaking the smoke test, temporarily add the call at the end of `nalar_demo`, boot, confirm QEMU exits on its own, then remove it (do NOT leave it in — it kills the normal boot).

## Ring-3 apps must re-enable interrupts on exit (do not regress)

`usermode::enter_user` calls `x86_64::instructions::interrupts::enable()` after the app returns. The EXIT syscall's entry clears IF (SFMASK) and its longjmp return path (`return_to_kernel`) never restores it, so without this the kernel runs with interrupts disabled after any ring-3 app exits — the timer stops, and `desktop_loop`'s `hlt` halts forever (this silently broke the interactive desktop/screensaver/animations from v0.4 until v0.11, since only static screenshots were checked).

## Desktop shell (v0.16 UX pass): Start menu + desktop icons + tray clock

`wm.rs` renders a modern desktop shell blending macOS / Ubuntu-GNOME / Windows XP: a gradient **taskbar** with a green **Start button**, rounded running-window buttons, and a right **tray** (theme name + a live **CMOS-RTC clock** via `read_clock` + workspace pips); a **Start menu** (opens on the Start button) listing the launchable apps (`LAUNCH`) plus power actions; and **desktop icons** (top-left) that launch on double-click. The WM cannot run a ring-3 app itself, so `shell_click` queues the request in `pending`/`pending_power`; `desktop_loop` drains it via `wm::take_pending_launch()` → `app::run_named` (and `take_pending_power()` → `power::shutdown`/`restart`). Rendering helpers: `gradient_v`, `fill_rounded_gradient`, `darken`, `lerp`. Verified headlessly by `wm::self_test()` (drives `handle_mouse` to open the menu and click a row, asserting the launch is queued) → `MILESTONE: DESKTOP SHELL OK`; screenshot `docs/img/desktop-shell.png`. Keep `taskbar_click`'s button strip geometry in sync with `draw_taskbar` (it starts after the Start button at `6 + START_W + 8`, 116 px pitch).

## desktop_loop drives animation + screensaver

`desktop_loop` wakes on each timer tick (`hlt`), tracks idle time, and: activates the screensaver after ~12 s idle (dismiss on input), re-renders on input or while a micro-interaction animates (`wm::tick_animation`), and routes keyboard→terminal / mouse→window-manager. It needs the timer alive (see the interrupts note above).

## Freestanding C# apps have no GC (important for app authors)

Ring-3 C# apps compile with bflat `--stdlib:zero` (zerolib) + the `bzstart.rs` shim. **As of v0.15 increment 4, heap allocation works**: `SystemNative_Malloc` is now a growable, zeroed mmap-backed bump heap, and zerolib's `new`/`RhpNewArray` route through it — so **`new`, arrays, heap objects, and generics work** (verified by `heap.cs`). The remaining hard limits are all rooted in **uninitialized GC statics**: (a) a `static` field holding a reference reads garbage (XOX first hit this: a `static readonly char[] _board = new char[9]` faulted at a wild address) — keep heap state in **locals**, not `static` refs; (b) a **method-group→delegate** conversion caches the delegate in a GC static and faults — use **function pointers** (`delegate*<int,bool>` + `&Method`) instead (hit in `bcl.cs`); (c) storing a reference into an **object-array element** (`_arr[i] = obj`) emits `stelem.ref`→`RhpStelemRef`, whose array-covariance check faults — store references into **object fields** instead (a **linked list** of nodes works, `RhpAssignRef` is fine), which is why `Buitenzorg.UI`'s `UIElement` keeps children as a linked list, not `object[]` (hit building the UI toolkit in v0.16). There's also no reclaiming GC yet (bump-only — memory is freed only when the app exits). Full GC + static-ref init arrives with the NativeAOT `--stdlib:dotnet` link. More v0.9-era gotchas along the same theme (still apply for the freestanding string path): (1) `stackalloc T[n]` of a **struct** array emits a checked size multiply needing `ThrowOverflowException`, which zerolib lacks and bflat then fails codegen on — use a constant-size `stackalloc byte[N]` and cast the pointer instead; (2) larger structs/buffers make the compiler emit `memset`/`memcpy`/`memmove`/`memcmp`, so the shim now provides those; (3) no `new string(...)` / `ToString()` / string concat — build text into `stackalloc char[]` and draw with `Graphics.DrawChars`. Full GC/CoreCLR is the remaining Layer-4 "Menengah" work.

## Scheduler preemption (important)

Timer-driven preemption is **off by default** and enabled only for `scheduler_demo` (the v0.2 milestone needs busy-wait tasks to alternate); it is turned back off immediately after. Everything else (dahan services, aio worker, desktop) runs cooperatively via `task::yield_now`. Do not re-enable preemption globally: switching tasks in the middle of the boot task's heavy heap/framebuffer work corrupted memory (a race between `context_switch`, cross-task `without_interrupts`, and the console/heap), which showed up as a rodata dump + a cascade of exceptions. If you add long-running kernel tasks that must run concurrently, make them cooperative, not preemptive.

After the boot demos, `desktop_loop` is the final loop: it drains the keyboard queue into the terminal and polls the PS/2 mouse into the window manager, recomposing when anything changes. The framebuffer console and the compositor share the hardware framebuffer; once the desktop renders it covers the boot text, and the full boot log always remains on serial.

## Kernel demo flow (main.rs)

`kernel_main` runs a sequence of milestone demos, each printing `MILESTONE: X OK` markers the smoke tests grep for: `scheduler_demo` (v0.2) → `batang_demo` (v0.3: PCI/storage/mouse/pixels) → `tunas_demo` (v0.4: run HELLO.ELF in ring 3) → `dahan_demo` (v0.5: VFS+FAT-write, service manager, aio benchmark, net, run SVC.ELF as a service). Ring-3 programs run one at a time via `run_user_elf` (load → enter_user → unmap); it must only be called when the boot task is the sole runnable task (preemption is inert then), so `dahan_demo` drains pending tasks with `yield_now` before launching the C# service. Two RAM disks live on the heap (hence the 16 MiB heap): one FAT12-formatted for `/ram`, one for the aio benchmark.

## Building the C# user program (v0.4)

`scripts/build-hello-csharp.ps1` compiles `userland/hello-csharp/hello.cs` with **bflat** (`--stdlib:zero`, NativeAOT/ILC, no GC/JIT) to an object, compiles `bzstart.rs` (freestanding `_start` + `SystemNative_*` PAL shim that calls Buitenzorg syscalls) with rustc for `x86_64-unknown-none`, and links both with **rust-lld** into a static ELF at `0x400000`. `bzimage/build.rs` embeds that ELF as `HELLO.ELF`; `build.ps1` runs the C# build first when `tools/bflat` is present (both are gitignored). Details: `docs/csharp-userland.md`.

## Ring-3 page-table gotcha (do not regress)

To execute a user page, **every** level of the page-table walk must have `USER_ACCESSIBLE` set (permission is the AND of all levels) **and** must not have `NX` set (an instruction fetch faults if NX appears at any level). Upper tables are shared with kernel mappings that lack USER, and the bootloader's low PML4 entry has NX set. `memory::make_user_path` ORs in USER and clears NX on the intermediate entries along the path to each user page, then flushes the TLB. `elf::load` and `memory::map_user_region` both call it. Leaves keep their own bits, so kernel memory stays protected. `requirements.md` (in Indonesian) is the authoritative spec; its §17 checklist tracks progress and must be updated (with honest annotations) when items land.

## Build & Test Commands

```powershell
.\scripts\quickstart.ps1                  # install ALL deps (Rust/.NET/QEMU/bflat) + build + boot (Linux: quickstart.sh)
.\scripts\build.ps1                       # everything → dist/*.img  (Linux: build.sh)
.\scripts\run-qemu.ps1 [-Uefi]            # boot in QEMU with display + serial
.\scripts\smoke-test.ps1                  # headless boot, assert milestone markers (CI: smoke-test.sh)
.\scripts\make-vm-images.ps1              # raw img → VMware .vmdk + VirtualBox .vdi + Hyper-V .vhdx + .vmx  (Linux: .sh)
.\scripts\make-hyperv-vm.ps1              # create a Gen-1 Hyper-V VM from the VHDX (elevated; guarded, manual fallback)
.\scripts\flash-usb.ps1 -List             # write a bootable image to USB (real hardware); Linux/macOS: flash-usb.sh
.\scripts\bench.sh                        # perf regression gate (boot-to-READY + async-IO ops/s vs budget; CI job)
cd kernel; cargo test -p bz-abi           # Rust-side ABI contract tests
cd kernel; cargo run --release -p bzimage -- --run   # rebuild + boot in one step
dotnet build Buitenzorg.slnx              # C# runtime + SDK (note: .slnx, not .sln)
dotnet test Buitenzorg.slnx               # C#-side ABI contract + manifest tests
dotnet run --project sdk\bz -- help       # bz CLI
```

## Hard-won Build Gotchas

- **Never build `bzkernel` for the host.** It is deliberately excluded from workspace `default-members`; building it without `--target x86_64-unknown-none` hits an LLVM error ("offset is not a multiple of 16", x86-interrupt ABI on MSVC). `cargo build` at `kernel/` root builds `abi` + `bzimage`, and `bzimage` pulls the kernel in as an artifact dependency with the right target (requires nightly + `bindeps`, pinned via `rust-toolchain.toml` / `.cargo/config.toml`).
- QEMU on this machine lives at `C:\Program Files\qemu\` (not on PATH); `bzimage` and the scripts auto-detect it, override with the `QEMU` env var. `QEMU_EXTRA="-s -S"` adds GDB debugging.
- OVMF firmware for UEFI runs is auto-downloaded by `ovmf-prebuilt` into `kernel/target/ovmf/`.
- Keep kernel console output ASCII-only: the framebuffer font covers basic latin, and serial logs read cleanly.
- The GDT must reload SS/DS/ES after `CS::set_reg` (stale bootloader selectors #GP on the first interrupt) — don't "simplify" that away.

## ABI Contract (critical invariant)

`kernel/abi` (`bz-abi`) is the single source of truth for the Rust↔C# boundary; `runtime/Buitenzorg.Runtime/Sys/` mirrors it. Any ABI change must touch **both sides + both contract test suites (`cargo test -p bz-abi`, `AbiContractTests.cs`) + `docs/abi.md`** in the same change. Syscall numbers are append-only. `Buitenzorg.Runtime` must stay NativeAOT-compatible (no dynamic reflection; JSON via source-generated context).

## What Buitenzorg Is

A hybrid, AI-native operating system:

- **Rust** (`no_std`) for the "unsafe world": bootloader (UEFI + legacy), kernel (ring 0), memory manager, scheduler, interrupts, and hardware drivers (NVMe/SATA/IDE/USB storage, GPU, input, network, audio).
- **C#/.NET** for the "managed world" (ring 3): system services, compositor/window manager, desktop environment, AI subsystem, SDK, and apps. Runtime strategy: NativeAOT first, CoreCLR/JIT later, hybrid long-term.
- **AI-native**: local LLM engine, computer vision, and GenAI (image/audio/video) are OS-level services (Layer 6) with a Hugging Face-integrated Model Manager — not add-on apps.
- **Polyglot app model**: one manifest/app model, four app variants (console, desktop, web, widget), writable in C#, JS, TypeScript, or Python.
- Unlike Cosmos (C# kernel via IL2CPU), Buitenzorg keeps the kernel in Rust and gives C# the full .NET runtime in user-space.

## Architecture (10 layers, bottom-up)

Hardware (x86-64 → ARM64 → RISC-V) → Bootloader (Rust) → Kernel (Rust) → Driver subsystem (Rust, some C# user-space) → Managed runtime (.NET, the critical Rust↔C# bridge) → System services (C#) → AI subsystem (C#) → Desktop environment (C#) → App framework/SDK (polyglot) → Applications.

**Interop rules (Layer 4 is the make-or-break layer):**
1. All cross-language calls go through the C ABI (`extern "C"` + P/Invoke/syscall).
2. Only primitives, pointers, and `#[repr(C)]` structs cross the boundary.
3. Syscalls form a stable numbered table (Linux-style).
4. Large data (framebuffers, files, AI tensors) uses zero-copy shared memory, never marshalled copies.
5. Managed objects are GC-pinned while their pointers are held by Rust.

## Repo Structure

- `kernel/` — Rust nightly workspace: `abi` (ABI contract), `bzkernel` (ring-0 kernel: main/serial/framebuffer/gdt/interrupts/memory/allocator/syscall/logo), `bzimage` (disk-image builder + QEMU runner). Key crates: `bootloader` 0.11, `x86_64` 0.15, `spin`, `pic8259`, `pc-keyboard`, `linked_list_allocator`, `noto-sans-mono-bitmap`.
- `runtime/` — `Buitenzorg.Runtime` (ABI mirror, `BzSys` facade with native-P/Invoke + host-simulation backends, `AppManifest`), tests, `samples/HelloBuitenzorg` (the "Hello from C#!" milestone program, runs on host sim today).
- `sdk/` — `bz` CLI (implemented: `new`, `manifest validate`, `version`; other subcommands are roadmap stubs), `templates/console-csharp`, `manifest.schema.json`.
- `ai/`, `apps/` — placeholders with READMEs; work starts at v0.12 and v0.16 respectively.
- `scripts/`, `.github/workflows/ci.yml`, `docs/` (`abi.md`, `getting-started.md`, `first-app.md`, `run-in-vm.md`, `install-hardware.md`, `debugging.md`), `dist/` (gitignored image output). Onboarding scripts: `quickstart.ps1`/`.sh` (install deps + build + boot), `make-vm-images.ps1`/`.sh` (VMware `.vmdk` + VirtualBox `.vdi` + Hyper-V `.vhdx` — the VHDX is pre-created at a whole 64 MiB then filled via `qemu-img convert -n`, because a bare `-O vhdx` yields an odd 5.47 MiB size Hyper-V rejects and this qemu build can't `resize` a vhdx), `make-hyperv-vm.ps1` (create a Gen-1/BIOS Hyper-V VM from the VHDX — Gen 2 is UEFI+SCSI, wrong for the MBR disk; guarded on the Hyper-V module + admin, prints manual steps otherwise), `flash-usb.ps1`/`.sh` (write a bootable image to a USB stick for real-hardware boot — guardrailed: removable-only, refuses the system disk, typed confirm, read-back verify; `-List`/`--list` to enumerate). Real-hardware boot is prepared but **not yet validated on a physical machine** — see `docs/install-hardware.md` for the honest capability matrix (PS/2 not USB HID, IDE-PIO not NVMe, PIC/PIT not APIC). The VS Code extension (`sdk/vscode-extension`) has commands for new-project (template picker), build&run, deploy, GDB debug, manifest-validate; full DAP breakpoint debugging is still TODO.
- `tools/MagicAppGen` — **Magic App Generator**, a host-side Avalonia UI code editor (net10.0, Avalonia 11.2 + AvaloniaEdit + Semantic Kernel 1.61) whose AI assistant **"Jack - The Code Bender"** generates Buitenzorg apps from a prompt. Multi-provider LLM (OpenAI / Claude / Gemini / Ollama — Claude bridged from `Anthropic.SDK`'s `IChatClient`), every setting in `app.config` and editable from the Settings dialog. Kernel functions (`Ai/BuitenzorgPlugin.cs`) give the model the **exact API signatures** + zerolib gotchas, scaffold/read/write project files, and run `scripts/build.ps1` / `scripts/smoke-test.ps1`; common functions (`Ai/CommonPlugin.cs`) add Tavily search, page scraping, math, and date/time. `Services/ProjectTemplates.cs` holds 8 starter templates (console / desktop-ui / drawing / game / audio in C#, plus js/ts/python) — **the five C# ones are verified to compile with real bflat `--stdlib:zero`**, which is how a batch of wrong-argument-order calls got caught. Headless modes `--list-templates` / `--scaffold <id> <dir> [Name]` allow verification without a display. `tools/` is gitignored except this project (bflat lives there too).

## Roadmap Order

Versions are codenamed after plant growth stages (homage to Bogor Botanical Gardens). The dependency order matters:

1. **v0.1–0.4** — bootloader → kernel core → drivers/boot media → managed runtime bring-up ("Hello from C#")
2. **v0.5–0.7** — VFS/services/async I/O → graphics/window system → desktop environment + terminal
3. **v0.8–0.10** — app framework + VS Code tooling (DAP debugging, debug bridge) → web/widget apps → theme engine (8 built-in themes) + package manager
4. **v0.11 GPU acceleration must land before v0.12 AI subsystem** (AI depends on the compute API; CPU fallback required)
5. **v0.13–0.16** — virtualization → polyglot runtimes → **v0.15 "Matang" managed runtime C# lengkap (GC + full .NET BCL: LINQ/Regex/Tasks/…) — prasyarat** → v0.16 preloaded suite + optimization pass
6. **v1.0** — stable x86-64 release; **v1.x** — ARM64, then RISC-V ports

`requirements.md` §17 is the development checklist — mark items `[x]` there as they are completed.

## Design Principles to Uphold

- Microkernel-leaning: drivers/services in user-space where possible, so crashes are isolated.
- Performance is policy, not afterthought: async io_uring-style I/O, zero-copy paths, parallel dependency-aware init, benchmark regression checks in CI.
- HAL is multi-arch from day one (x86-64 first, but don't hardcode against ARM64/RISC-V portability).
- Offline-first AI with per-app permission broker (camera/mic/model access).
- Theme engine is token-based (design tokens, hot-reload, `.theme` packages, dark/light variants).

## Conventions

- `requirements.md` and its version codenames are in Indonesian; keep them intact when editing that file.
- The `bz` CLI is the OS's unified tool name (apps/packages/themes/AI models/VMs) — use it for any CLI design.
- A `frontend-design` skill exists in `.claude/skills/` for UI/visual design work (themes, desktop shell, web content).
