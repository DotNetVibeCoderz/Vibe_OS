<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:svc="using:MagicAppGen.Services"
        x:Class="MagicAppGen.Views.NewProjectDialog"
        Width="560" Height="440" CanResize="False"
        WindowStartupLocation="CenterOwner"
        Title="New Project From Template" Background="#101512">
  <DockPanel Margin="14">
    <StackPanel DockPanel.Dock="Top" Spacing="8">
      <TextBlock Text="Pick a template" FontWeight="Bold" Foreground="#5FD46E" />
      <Grid ColumnDefinitions="110,*">
        <TextBlock Grid.Column="0" Text="App name" VerticalAlignment="Center" />
        <TextBox Grid.Column="1" x:Name="NameBox" Text="MyApp" />
      </Grid>
    </StackPanel>

    <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal"
                HorizontalAlignment="Right" Spacing="8" Margin="0,12,0,0">
      <Button Content="Cancel" Click="OnCancel" />
      <Button Classes="primary" Content="Create" Click="OnCreate" IsDefault="True" />
    </StackPanel>

    <ListBox x:Name="TemplateList" Margin="0,10,0,0" Background="#151C17">
      <ListBox.ItemTemplate>
        <DataTemplate x:DataType="svc:ProjectTemplate">
          <StackPanel Margin="4">
            <StackPanel Orientation="Horizontal" Spacing="8">
              <TextBlock Text="{Binding Title}" FontWeight="Bold" Foreground="#EAF1EB" />
              <Border Background="#1E3A24" CornerRadius="4" Padding="5,1">
                <TextBlock Text="{Binding Language}" FontSize="11" Foreground="#8FE39B" />
              </Border>
            </StackPanel>
            <TextBlock Text="{Binding Summary}" FontSize="12" Foreground="#92A899" TextWrapping="Wrap" />
          </StackPanel>
        </DataTemplate>
      </ListBox.ItemTemplate>
    </ListBox>
  </DockPanel>
</Window>
